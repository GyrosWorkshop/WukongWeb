import lightBaseTheme from 'material-ui/styles/baseThemes/lightBaseTheme'
import {merge} from 'lodash'

export default merge(lightBaseTheme, {
  palette: {
    primary1Color: '#f09442',
    primary2Color: '#e95d3b',
    accent1Color: '#99dc3c',
    pickerHeaderColor: '#f09442',
    footerColor: '#375ca0'
  }
})
